// Package kafkautil wraps the project's Kafka producer/consumer
// patterns: idempotent producers, retry-with-backoff consumers,
// schema registry hooks, and structured event envelopes.
package kafkautil

// Version is the wire-protocol version this package targets.
const Version = "3.7"
