# DB Version: 18
# Performance Tuning for Distributed Ledger

# Required when this file is mounted into the postgres container — without
# it PG defaults to listen_addresses='localhost' and the container's
# external port mapping has nothing to forward to.
listen_addresses = '*'

shared_buffers = 4GB
effective_cache_size = 12GB
work_mem = 64MB
max_connections = 300
random_page_cost = 1.1

# PostgreSQL 18 AIO Tuning
io_method = worker
io_workers = 8
io_combine_limit = 128
max_wal_size = 4GB
min_wal_size = 1GB
